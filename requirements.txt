# Essential packages for basic functionality
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0

# Excel file processing (required)
openpyxl>=3.1.0
xlrd>=2.0.0

# Date handling
python-dateutil>=2.8.0

# Optional visualization (install if available, graceful fallback if not)
plotly>=5.15.0

# Optional mapping (install if available)
folium>=0.14.0
streamlit-folium>=0.13.0

# RAG System Dependencies (Optional - only for AI Assistant Mode)
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2
groq>=0.4.1
torch>=2.0.0
transformers>=4.30.0

# For deployment stability
protobuf<4.0.0